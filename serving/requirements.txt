# Minimal dependencies for the inference service.
#
# Deliberately excludes torch, transformers, pandas and tqdm: those belong to
# training and to the FinBERT service. The ONNX backend needs none of them,
# and installing torch alone added ~2 GB and roughly 25 minutes to the image
# build on an ARM laptop.
#
# scikit-learn is required despite not being imported: scaler.pkl is a pickled
# sklearn MinMaxScaler and cannot be unpickled without it.
flask>=3.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
onnxruntime>=1.16
prometheus_client>=0.19
gunicorn>=21.0
