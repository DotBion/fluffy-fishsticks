# Installed into the Airflow containers at startup (see docker-compose.yml).
alpha_vantage
pandas
minio
nltk
scikit-learn

# Optional market-data provider; alpha_vantage stays the default.
# yfinance
